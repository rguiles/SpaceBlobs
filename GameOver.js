class GameOver extends Phaser.Scene {
  constructor() {
    super('GameOver');
  }

  init() {
  }

  preload() {
  }

  create() {
    this.config = this.game.config;
  }

  update() {
  }
}
